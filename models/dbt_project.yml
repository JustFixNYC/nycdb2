name: nycdb2
version: "0.1.0"
config-version: 2
profile: opendata_etl_nycdb2

model-paths: [models]
test-paths: [tests]

target-path: target
clean-targets:
  - target
  - dbt_packages

vars:
  target_schema: nyc_housing

models:
  nycdb2:
    +materialized: view
